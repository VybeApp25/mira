<Window x:Class="Mira.Windows.App.Shell.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Mira.Windows.App.Shell"
        mc:Ignorable="d"
        Title="Mira Guidance"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">
    <Canvas x:Name="RootCanvas">
        <local:PointToCanvas x:Name="PointTo" />

        <Border x:Name="HighlightBox" Visibility="Collapsed" IsHitTestVisible="False"
                CornerRadius="6" BorderThickness="2.5" BorderBrush="#2563EB" Background="#182563EB" />

        <Border x:Name="LabelBadge" Visibility="Collapsed" IsHitTestVisible="False"
                CornerRadius="5" Background="#2563EB" Padding="8,4">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.30" BlurRadius="6" ShadowDepth="2" />
            </Border.Effect>
            <TextBlock x:Name="LabelText" Foreground="White" FontSize="11" FontWeight="SemiBold" />
        </Border>

        <Border x:Name="ExplanationBadge" Visibility="Collapsed" IsHitTestVisible="False"
                CornerRadius="8" Background="#F0101216" BorderBrush="#4D2563EB" BorderThickness="1"
                Padding="12,8" MaxWidth="340">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.40" BlurRadius="8" ShadowDepth="3" />
            </Border.Effect>
            <TextBlock x:Name="ExplanationText" Foreground="White" FontSize="12" TextWrapping="Wrap" TextAlignment="Center" />
        </Border>
    </Canvas>
</Window>
