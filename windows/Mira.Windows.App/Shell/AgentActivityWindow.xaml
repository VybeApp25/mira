<Window x:Class="Mira.Windows.App.Shell.AgentActivityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Mira Activity"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        SizeToContent="Height"
        Width="300">
    <StackPanel x:Name="ChipsPanel" Margin="0,0,0,0">
        <ItemsControl x:Name="StepsList">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="#DD17191B" CornerRadius="10" Padding="10,7" Margin="0,0,0,6"
                            BorderBrush="#402563EB" BorderThickness="1">
                        <TextBlock Text="{Binding}" Foreground="#ECEEED" FontSize="12" TextWrapping="Wrap" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</Window>
