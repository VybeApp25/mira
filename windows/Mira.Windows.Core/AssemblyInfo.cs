using System.Runtime.Versioning;

// Declares this assembly as Windows-only for the platform-compatibility analyzer
// (CA1416). The <SupportedOSPlatform> MSBuild property in the .csproj documents
// the same intent for humans reading the project file; this attribute is what the
// analyzer actually keys off, so both are kept in sync deliberately.
[assembly: SupportedOSPlatform("windows")]
