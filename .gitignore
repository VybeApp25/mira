.DS_Store
AgentService/node_modules/
AgentService/.env
shared/codegen/node_modules/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
build/
DerivedData/
.build/
*.o
*.d
Mira/Config/AppSecrets.swift
AgentService/dist/

# Local xcodebuild output
build_local/
build_run/
build_qa/
build_release/

# Release artifacts (notarized output of build_release.sh)
Mira-*.dmg
Mira-*.zip
rw.*.dmg

# Local tooling / agent state
.claude/
.agents/
.pi/
.hermes/
.netlify/
skills-lock.json
supabase/.temp/

# SourceKit-LSP build-server config (xcode-build-server). Machine-specific:
# holds absolute DerivedData paths, so each dev regenerates it locally with
#   xcode-build-server config -project Mira.xcodeproj -scheme Mira
buildServer.json

# .NET build output (windows/) — standard bin/obj, kept out of source control the
# same way Mira.xcodeproj's build/DerivedData are above.
windows/**/bin/
windows/**/obj/
